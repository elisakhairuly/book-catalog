import { HighRating } from './high-rating';

describe('HighRating', () => {
  it('should create an instance', () => {
    const directive = new HighRating();
    expect(directive).toBeTruthy();
  });
});
